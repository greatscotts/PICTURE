<li>
<%= link_to image_tag user.image_url, :size => "300x300", :id=>"profilepicture" %>
<%= link_to user.username, user %>
<% if current_user.admin? && !current_user?(user) %>
  | <%= link_to "delete", user, :method=> :delete,
    :data=> { :confirm=> "You sure?" } %>
<% end %>
</li>
